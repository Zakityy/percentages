var name = prompt("What is your name?");
if (name == null || name == "") {
    txt == "No name given";
} else {
    txt = "Hello, " + name + "!";
}
alert(txt)

alert('My name is bobby');
var name = prompt('what do you like to do for a living?')
if (name == null || name == "") {
    txt == "No answer given";
} else {
    txt = "thats so interesting!";
}
alert(txt)

alert('Im a browser I cant really do anything :(');

var name = prompt('Are you a only child?')
if (name == null || name == "") {
    txt = "No answer given";
} else {
    txt = "nice!";
}
alert(txt)